version: '3.7'
services:
  database:
    image: 'mysql:8'
    environment:
      MYSQL_ROOT_PASSWORD: nexus123
    ports:
      - '3306:3306'

  test_database:
    image: 'mysql:8'
    environment:
      MYSQL_ROOT_PASSWORD: nexus123
    ports:
      - '3206:3306'
